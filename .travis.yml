language: java
jdk:
  - openjdk8

jobs:
  include:
    - stage: test
      script: ./gradlew test
    - stage: integration test
      script: ./gradlew testInteg
    - stage: docker hub push
      if: branch = master
      script:
        - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
        - ./gradlew dockerPush
